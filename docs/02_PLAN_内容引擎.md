# 内容引擎执行计划

> 版本：Web-first v1.0
> 更新：2026-07-24
> 原则：每个阶段必须跑通用户闭环并验收，再扩展能力；不以页面数量作为进度。

## 1. 执行总览

```text
阶段 0：Web 基线
阶段 1：核心 Agent 与情报到选题
阶段 2：选题到三平台内容包
阶段 3：发布与复盘
阶段 4：飞书、账号与媒体生产增强
```

当前阶段 0 的功能基线已完成，但“多用户交叉访问隔离测试”尚未关闭；阶段 1 可开始实现，但该安全验收必须在阶段 1 首个合并前补齐。P0 仅支持单 Owner 工作空间，不包含成员邀请、分配、审核或评论。

## 2. 阶段 0：Web 基线

### 目标

建立可安全承载用户数据与外部能力的 Web 运行环境。

### 已完成

- React/Vite Web 前端与 Fastify API。
- 用户注册、登录、JWT 会话和工作空间隔离。
- PostgreSQL 迁移、Redis/BullMQ、Docker 本地编排。
- 服务端加密凭据库，浏览器不保存 Tavily/百炼 Key。
- 服务端 RSS 刷新、公开链接剪藏、Tavily 搜索 API。
- 17 个合规自动 RSS 目录、7 个辅助渠道、批量接入与独立失败状态。
- 13 类确定性分类、真实命中关键词持久化、规范化 URL 去重，以及题材筛选与关键词搜索。
- 百炼 CLI Worker 和异步任务队列骨架。
- Web 注册、首次设置、状态保存与真实 RSS 刷新验收。

### 尚未完成

- 工作空间快照尚未完全拆分为正式领域表。
- Tavily 和公开链接剪藏需要用户真实 Key/链接验收。
- 百炼 Worker 仅有任务骨架，尚未形成内容项目 Agent。
- 多用户工作空间交叉访问自动化测试尚未完成。

### 验收门槛

- 新用户可注册、进入工作空间、保存刷新后的来源和热点。
- RSS 刷新成功、失败和空结果均可识别。
- 用户 Key 在刷新页面后不回显。

## 3. 阶段 1：核心 Agent 与情报到选题

### 用户闭环

```text
配置题材与来源 → 刷新/检索/剪藏 → 情报收件箱 → 核心 Agent 计划 → AI 分析 → 创建选题 → 选题确认
```

### 工作项

1. 建立 Skill 注册表、Skill 版本、题材包、Agent 计划、确认卡和生成运行记录数据模型。
2. 建立核心 Agent：模型规划、Skill 白名单校验、结构化计划、取消与确认接口；不允许自由工具调用。
3. 将题材、资讯来源、关键词和刷新策略从工作空间快照拆为领域 API 与表。来源与情报已迁移；独立题材配置仍待完成。
4. 增加定时刷新 Worker、去重、失败重试和来源健康状态。规范化 URL 去重和单次刷新失败隔离已完成；定时调度和重试仍待完成。
5. 完成 Tavily Key 的真实保存、搜索和“加入热点池”验收。
6. 完成公开链接剪藏：可读公开页预读；验证码、登录、付费墙给出明确可行动提示。
7. 接入百炼 CLI 的“情报分析、来源核验、选题评分” Skill，输出摘要、热度、建议角度和待核验事实。
8. 实现选题池、选题评分和从情报创建选题。

### 不做

- 不做飞书自动同步。
- 不做视频、数字人、生图或复杂账号授权。
- 不允许 AI 自动把搜索结果直接变成正式选题。

### 验收门槛

用户使用自己的来源和 Tavily Key，完成一次：RSS/搜索/剪藏中的任一情报 → AI 分析 → 创建并确认一个选题。

## 4. 阶段 2：选题到三平台内容包

### 用户闭环

```text
确认选题 → 创建内容项目 → 项目 Agent → 文案与平台版本 → 审核 → 导出内容包
```

### 工作项

1. 建立正式的内容项目、来源引用、会话、Agent 工具调用、平台版本、生成运行记录和资产引用数据模型。
2. 实现项目内通用 Agent，不再新增孤立的“改写/解读”页面。
3. 接入百炼 CLI 文本任务：研究、解读、改写、长文写作、平台适配和口播稿。
4. 建立模型 Scope、模型选择、调用确认、用量日志和失败重试。
5. 支持公众号、小红书、视频号三种内容包和事实核验清单。
6. 支持正文编辑、版本保存、人工修改和导出。

### 不做

- 不做自动发布。
- 不做复杂视频成片。
- 不让 Agent 在缺少来源或用户确认时编造事实、直接覆盖人工版本。

### 验收门槛

用户从一个真实选题创建项目，通过 Agent 生成三平台初稿，人工修改后导出内容包。

## 5. 阶段 3：发布与复盘

### 用户闭环

```text
内容包 → 发布预检 → 排期 → 用户确认发布 → 填写数据 → 复盘建议
```

### 工作项

1. 账号目标、发布日历、发布任务、状态机和预检规则；P0 账号只记录发布目标，不做 OAuth 授权。
2. 导出公众号 HTML/素材包、小红书图文包、视频号口播/字幕/视频素材包。
3. 手工确认发布和平台内容链接/ID 回填。
4. 手工录入基础指标，形成内容与题材复盘结论。
5. 只支持人工填写平台内容链接/ID 和基础指标；官方 OAuth/API 统一移至阶段 4。

### 验收门槛

用户完成一次真实人工发布，并在同一内容项目中记录发布链接、基础数据和一条复盘结论。

## 6. 阶段 4：增强能力

### 4.1 飞书与协作

- 飞书 OAuth、可编辑模板、创建 Base、字段映射和单向/双向同步策略。
- 飞书只镜像热点、选题、排期和审批信息；主库仍是 PostgreSQL。
- 团队成员、角色、审核与评论。

### 4.2 账号与半自动发布

- 按平台规则接入官方 OAuth/API。
- 浏览器扩展在用户已登录的后台页预填内容。
- 扩展不上传 Cookie，风险提示、验证码和最终发布由用户完成。

### 4.3 媒体生产

- 图像、ASR、语音、视频理解和数字人 API 适配器。
- Remotion/Hyperframer 云端渲染 Worker 与对象存储。
- 长耗时任务队列、取消、重试、成本预算和产物归档。

## 7. 每次迭代的固定流程

1. 明确本次只服务哪一段用户闭环。
2. 先更新 PRD 和实施方案中的对象、边界、接口和验收条件。
3. 实现 API、数据迁移和 Worker，再实现页面。
4. 用真实来源、真实用户账户和人工确认跑通一次。
5. 更新验收记录，区分“已实现”“已真实验收”“待用户验收”。
6. 推送 GitHub 后再进入下一项。

## 8. 部署策略

### 本地开发阶段

在本地完成每个闭环和自动化测试。当前 2 核 2G 阿里云服务器不承担本地开发和视频渲染。

### 个人测试环境

阶段 1 或阶段 2 跑通后部署。2 核 2G 只运行 Web 静态文件、API 和低并发文本任务；PostgreSQL/Redis 最好迁移到托管服务，媒体文件使用 OSS。

### 不允许的部署方式

- 在 2 核 2G 主机上并发跑数据库、Redis、视频渲染和多个 Worker。
- 在生产服务器上使用 Vite 开发服务器。
- 将用户密钥、Cookie 或未加密数据库备份提交到 Git。
# 2026-07-25 执行更新：模型配置闭环（已完成）

1. 为凭据库增加验证状态、最后检测时间和错误信息。
2. 将外部 API、模型目录、任务策略和调用记录迁移为 Web 服务端 API。
3. 账户目录和外部连接以 `/models` 返回为准；百炼异步媒体和语音模型只从官方核验的模型市场目录补充。完成凭据检测后才允许同步和分配。
4. 已使用真实百炼 Key 完成账号目录同步；下一项真实验收是为“热点分析”配置一个文本模型并执行一次受控任务。
5. 模型目录按账户目录、CLI 媒体能力和百炼模型市场分层展示。当前市场层采用 2026-07-25 官方控制台核验清单；后续增加百炼控制台授权后，再按用户账号动态同步市场开通状态。
6. 图片、音频和视频策略按具体操作拆分。当前已完成文生图、图生图、语音合成、语音识别、文生视频、首帧图生视频、首尾帧生视频、参考生成和视频编辑的模型路由；下一步接入对应任务执行器。
7. 已完成“模型与 API”页面归属整理：动态页头、页签级反馈、调用统计仅在调用记录、移除跨页连接概览，并统一 960px 配置表单与 1180-1280px 工作区宽度。下一步仍是执行一次受控热点分析任务，不新增页面堆叠。
8. 已完成资讯来源目录与热点筛选闭环：17 个自动 RSS、7 个辅助渠道、13 类内容分类、命中关键词持久化、来源/题材筛选、关键词搜索和近 30 天保留策略。公众号导入已真实验收，可直接作为受控热点分析输入；Tavily 真实搜索作为独立验收并行推进，不再阻塞热点分析。

# 2026-07-26 执行更新：纯 Web 工作区重构

1. 已将一级导航收敛为今天、发现、规划、创作、发布、复盘、素材库和设置，删除情报源、网页搜索、剪藏链接、模型与 API 的一级入口。
2. 已建立发现工作区：热点情报、网络搜索、导入链接共享同一局部导航。微博、今日头条、央视网、X、公众号、财经媒体和官方公告改为网络搜索来源范围下拉。
3. 已建立设置工作区：工作空间、资讯来源、模型与 API、飞书 Base、账号授权。资讯来源不再显示辅助渠道卡片。
4. 已增加资讯来源更新接口，支持来源名称、URL、题材、关键词、语言、启停和刷新频率真实持久化。
5. 已完成热点三列卡片、固定详情抽屉、已立项标签、确定性马卡龙配色和本地时区时间展示。
6. 已完成 1280、1024、768、390 四档浏览器验收，修复中屏旧侧栏规则覆盖内容的问题。
7. 已删除 `main.tsx` 中无引用的旧发现页、旧链接页、旧模型页、旧来源页和旧搜索页；模型与 API 工作区统一调用 Web 服务端接口，生产构建不再包含桌面端、剪藏链接或辅助渠道旧文案。
8. 不再继续改导航；“导入链接 → 正文预览 → 加入热点池”已完成，下一功能关口切换为实现并验收单条资讯 AI 热点分析。真实 Tavily 搜索作为独立采集验收保留。
9. 已使用真实公众号链接跑通“导入链接 → 正文预览 → 加入热点池”：修复机器人 UA 触发验证页、公众号页面体积限制、重定向验证页误保存、正文摘要为空和来源显示为旧剪藏文案的问题；轻量读取被风控时自动切换隔离 Chrome。当前已有真实资讯可用于热点分析验收。

# 2026-07-26 执行更新：AI 热点分析设计已确认

1. 第一版采用单条资讯结构化评分，不做热点聚类、批量分析或多 Agent 评审。
2. 五维评分、服务端权重、三档建议状态、用户勾选平台和选题预填协议已经确认。
3. 新增账号定位、目标受众和任务提示词模板；提示词采用“固定系统约束 + 可编辑业务模板 + 运行上下文”三层结构。
4. 实施顺序为数据迁移与 Schema、提示词设置、准备/确认/Worker 执行、结果展示与选题预填、自动化和真实模型验收。
5. 当前状态为“设计已确认，待实施”。详细规格见 `docs/superpowers/specs/2026-07-26-ai-intelligence-analysis-design.md`。
